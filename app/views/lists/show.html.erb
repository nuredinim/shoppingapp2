<h1>View List's <%= pluralize(@items.count, "Item") %></h1>
<table>
	<thead>
		<tr>
			<th>Name</th>
			<th>Price</th>
			<th>Quantity</th>
		</tr>
	</thead>
	<tbody>
		<% @items.each do |item| %>
		<tr>
			<td><%= item.name %></td>
			<td><%= item.price %></td>
			<td><%= item.quantity %></td>
		</tr>
		<% end %>
	</tbody>
</table>
<a href="/lists">View Lists</a>